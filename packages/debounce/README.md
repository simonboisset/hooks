# One hook : useDebounce

React hook for debounced state.

# Instalation

```bash
yarn add @onehook/debounce
```

# Usage

```ts
import useDebounce from '@onehook/debounce';

const [value, setValue] = useDebounce('initial state', 500);
```
